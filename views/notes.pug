extends layout.pug 

block content 
  div.container
    h1 All notes 
    if  deleted 
        p.success deleted

    each note in notes 
     div.note-wrapper
       a.note(href='/notes/${ notes.id }') #{ note.title }
       a.delete(href='/notes/${ note.id}/delete') delete